# Copy to .env for local development. Never commit .env.
# Server-side only (no VITE_ prefix) — read by api/copilot.js and never
# exposed to the browser. Get a free key at https://console.groq.com/keys
GROQ_API_KEY=your_key_here
